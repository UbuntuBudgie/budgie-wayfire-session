project(
  'wayfire-bridge',
  version: '0.1.0',
  meson_version: '>= 0.60'
)

python = import('python').find_installation('python3')

libexecdir = join_paths(get_option('prefix'), 'libexec', 'budgie-desktop')

subdir('src')
